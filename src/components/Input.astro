---
const { context, label, type, placeholder, ...props } = Astro.props;
---

<div class='flex flex-col gap-y-2'>
	<label for={context} class='text-md text-start'>{label}</label>
	<input type={type} id={context} name={context} class='w-full p-2 focus:outline-none rounded-xl text-gray-900' placeholder={placeholder} {...props} />
</div>
